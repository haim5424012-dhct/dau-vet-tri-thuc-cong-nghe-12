# QA — Thẻ màu hai cụm ngành

Preview đã hiển thị đầy đủ các nhóm N1–N4 với nhãn “Cụm 1 · Lâm nghiệp” và N5–N8 với nhãn “Cụm 2 · Thủy sản”. Giao diện có thêm chú giải bằng biểu tượng Leaf/Waves và nhãn chữ, nên không phụ thuộc riêng vào màu. Các nút nhóm dùng xanh lá cho Lâm nghiệp, xanh nước–teal cho Thủy sản; thẻ mã và thẻ chi tiết dùng cùng ngữ nghĩa màu. `pnpm check`, `pnpm test`, `pnpm build` đều đạt. Không ghi nhận lỗi TypeScript hoặc lỗi bố cục từ thay đổi này.
